[gd_resource type="Shader" format=2]

[resource]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform vec4 color : hint_color;
uniform float amplitude = 1.0;

void fragment(){
	ALBEDO = color.rgb;
	ALPHA = 1.0 - UV.x*amplitude;
}"
